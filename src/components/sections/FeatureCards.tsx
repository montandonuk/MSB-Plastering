import Container from '../Container'

// This component now just provides spacing below the Hero's overlapping feature cards
export default function FeatureCards() {
    return (
        <section className="bg-brand-cream pt-12 md:pt-36 lg:pt-40 pb-16 md:pb-20">
            {/* Spacing element - the actual cards are now in the Hero component */}
        </section>
    )
}
